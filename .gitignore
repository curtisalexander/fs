# Rust build output
/target/
**/*.rs.bk

# Model weights / data (downloaded, not committed — big & licensed)
/models/
*.gguf
*.safetensors
*.bin

# The book PDF (copyrighted — keep your own local copy; do not commit)
Inference Engineering.pdf

# Scratch artifacts (regenerate locally; do not commit bulky/generated blobs)
/scratch/
*.golden.npy

# Small, license-safe verification fixtures under tests/golden/ are intentional:
# they are committed so cargo test can run without Python/network access.

# Python / uv (scripts/)
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# NOTE: reference/ds4 is a git SUBMODULE (pinned), intentionally NOT ignored.
.diagram-review/
